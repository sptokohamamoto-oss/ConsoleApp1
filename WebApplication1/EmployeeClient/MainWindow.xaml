<Window x:Class="EmployeeClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EmployeeClient"
        mc:Ignorable="d"
        Title="社員管理システム（ローカルDB接続)"
        Height="450" Width="856">
    
<Grid Margin="10">
    <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    
    <!--タイトル-->
        <TextBlock Text="社員管理システム（ローカルDB接続）"
               FontSize="20"
               FontWeight="Bold"
               Margin="0 0 0 10"/>
    
        <!-- 入力欄 -->

        <Grid Grid.Row="1" Margin="0 0 0 10">
          

     

                <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="150"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="名前：" Grid.Column="0" VerticalAlignment="Center"/>
            <TextBox x:Name="NameTextBox" Grid.Column="1" Width="150"/>

            <TextBlock Text="部署：" Grid.Column="3" VerticalAlignment="Center"/>
            <TextBox x:Name="DepartmentTextBox" Grid.Column="4" Width="150"/>

            <TextBlock Text="入社日：" Grid.Column="6" VerticalAlignment="Center"/>
            <DatePicker x:Name="HireDatePicker" Grid.Column="7" Width="150"/>
        </Grid>
        


        <!-- ボタン行 -->
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    Margin="0 0 0 10">

            <Button x:Name="AddButton" Content="追加" Width="80" Margin="0 0 10 0" Click="AddButton_Click"/>
            <Button x:Name="UpdateButton" Content="更新" Width="80" Margin="0 0 10 0" Click="UpdateButton_Click"/>
            <Button x:Name="DeleteButton" Content="削除" Width="80" Margin="0 0 10 0" Click="DeleteButton_Click"/>
            <Button x:Name="ReloadButton" Content="再読込" Width="80" Click="ReloadButton_Click"/>
        </StackPanel>

        <!-- DataGrid -->
        <DataGrid x:Name="EmployeeGrid"
                  Grid.Row="3"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single">

            <DataGrid.Columns>
                <DataGridTextColumn Header="id" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Department" Binding="{Binding Department}" Width="*"/>
                <DataGridTextColumn Header="HireDate"
                                    Binding="{Binding HireDate,StringFormat=\{0:yyyy/MM/dd\}}"
                            Width="120"/>

            </DataGrid.Columns>

          
        </DataGrid>

    </Grid>
</Window>


